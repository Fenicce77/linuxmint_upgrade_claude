# Linux Mint Upgrade Runbook — 21.3 Virginia → 22.3 Zena

Upgrade plan generated by Claude on 2026-09-22 from a live read of
`piticli3-HP-Pavilion-Laptop-15-cs0xxx` (kernel 5.15.0-151-generic, 3,056
packages installed, 470 pending within 21.3, 15 third-party APT repos).

Covers, in order: flattening 21.3, Timeshift snapshot, quarantining
third-party repos (Docker, MongoDB, Percona, Node, FortiClient, browsers,
VS Code, etc.), running `mintupgrade` to land on Mint 22, re-pointing each
repo at the `noble` base, riding point releases to 22.3 via Update Manager,
and post-upgrade verification with a Timeshift rollback path.

**2026-09-23 addendum:** a GPU/bootloader/image-consistency risk audit,
added after diagnosing this machine's actual setup — NVIDIA DKMS with
`prime-select` pinned to nvidia-only (no Intel fallback), a leftover
`grub-pc` install alongside grub-efi, a pending CUDA 11.5 → noble-default
toolkit version jump, and Ubuntu 24.04's new `systemd-oomd` behavior
against this box's Docker/MongoDB/Percona services.

## Contents

- [`docs/mint-upgrade-guide.html`](docs/mint-upgrade-guide.html) — the full
  runbook. Self-contained (no external dependencies) — open directly in a
  browser.

No commands in this runbook were executed automatically; this is
documentation to work through manually, not a script.
